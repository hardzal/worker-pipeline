# Ground study guides only in client source material

The generated Study Guide may reorganize, summarize, and turn the supplied `content` into questions, but it must not introduce factual claims from the model's external knowledge. The `topic` provides context and naming only. This deliberately favors traceability and trust over richer but unverifiable output; insufficient material must be represented as a limitation rather than filled with invented facts.
